// Generated by CoffeeScript 1.6.3
var Assets;

Assets = (function() {
  function Assets() {
    this.img_suffix = '';
    if (window.devicePixelRatio > 1.0) {
      this.img_suffix = '-hd';
    }
  }

  Assets.prototype.load = function(callback) {
    var assetsToLoad, loader;
    assetsToLoad = ['assets/plop_anim' + this.img_suffix + '.json'];
    loader = new PIXI.AssetLoader(assetsToLoad);
    loader.onComplete = callback;
    return loader.load();
  };

  return Assets;

})();
